{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "uvicorn app.main:app --reload --log-config ./app/config/log.yaml",
            "problemMatcher": []
        }
    ]
}